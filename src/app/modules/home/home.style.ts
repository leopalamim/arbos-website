import styled from "styled-components";
import backgroundImage from "assets/img/img-background.png"

export const HomeWrapper = styled.div`
  background: url(${backgroundImage});
  background-size: 100% auto;
`